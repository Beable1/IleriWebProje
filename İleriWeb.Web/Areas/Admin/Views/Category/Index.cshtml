@using IleriWeb.Core.DTOs
@model List<CategoryDto>
@{
    ViewData["Title"] = "Index";
}



<table class="table table-striped">
    <thead>
        <tr>
            <td>Id</td>
            <td>Name</td>
            <td>Created Date</td>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.CreatedTime</td>
            </tr>
        }

    </tbody>

</table>

<a class="btn-link" asp-area="admin" asp-controller="Category" asp-action="Create">Add Category</a>
